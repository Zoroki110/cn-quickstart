[build]
# Build the active frontend only.
base = "quickstart/frontend"
publish = "quickstart/frontend/build"
# Print LoopWalletConnector head to verify correct source, then build.
command = "npm install && echo '--- LoopWalletConnector head ---' && sed -n '1,120p' src/wallet/LoopWalletConnector.ts && npm run build"

[build]
# Netlify build settings for the active frontend only.
# Ensures we build from the correct base directory and publish the right bundle.
base = "quickstart/frontend"
publish = "quickstart/frontend/build"
command = "npm install && echo '--- LoopWalletConnector.ts (head) ---' && sed -n '1,120p' src/wallet/LoopWalletConnector.ts && npm run build"


